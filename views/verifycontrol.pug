extends layout

include menu-partial 

block content   
        +admin-list
        h3 Контроль верификации
        div(id='forData' class='container-fluid')
            form(name='projects' action='/admin/verifycontrol/onhand' method='get' )
                div(class='form-group row')
                        label(class="col-sm-3 col-form-label") Проекты:
                        div(id='forProjectList' class='col-sm-6')
                            select(id='prjlst' class="form-control")
                                each project in !{projects}
                                    option(value=project.project_name)= project.project_name
                                else
                                    option(value='null' selected='selected') null
                div(class='form-group row')
                        label(class="col-sm-3 col-form-label") Предмет:
                        div(id='forSelectSbjs' class='col-sm-6')
                            select(id='sbjlst' class="form-control")
                                each subject in !{subjects}
                                    option(value=subject.subject_code)= subject.subject_code 
                                else
                                    option(value='null' selected='selected') null
                div(id='forTable' class='container-fluid') 
            form(action='/admin/verifycontrol/onhand')
                button(id="goToVerifyButton" type="submit"  class='btn btn-primary') Проверить
        script.
           $(document).ready(function(){     
               $.ajax({
                   type: 'get',
                   url: '/admin/verifycontrol',
                   dataType: 'json',
                   success: function(data){
                       
                   }
               })            
               $('#sendResult').click(function(e){
                    e.preventDefault();
                    let resultOfVerifyControl = $("td[class=\'img\']");
                    let data = new Object();
                    data.id = $("td[class=\'img\']").attr('id');
                    data.status = $("td[class=\'img\']").attr('status');
                    $.ajax({
                        type: 'post',
                        url: '/admin/verifycontrol/sendResult',
                        data: data,
                        dataType: 'json',   
                        success: function(data){
                            
                        },
                        error: function(info){}
                    });
                });

                $('#goToVerifyButton').click(function(e){
                    e.preventDefault();
                    let values = $("input[type=\'checkbox\']:checked");
                        if(values.length){
                            let data = new Object();
                            data.values = new Object();
                            data.project = $('#prjlst').val();
                            data.subject = $('#sbjlst').val();
                            for(i = 0; i < values.length;i++){
                                data.values[i] = $('#'+values[i].id).val();
                            }
                            localStorage.setItem('data', data);
                            $.ajax({
                                type: 'post',
                                url: '/admin/verifycontrol/onhand',
                                data: data,
                                dataType: 'json',
                                success: function(data){
                                    /*let carousel = '<div id="carouselhr" class="carousel slide" data-ride="carousel">';
                                    carousel += '<div class="carousel-inner">';

                                    carousel += '<div class="carousel-item active" data-interval="false" >';
                                    carousel += '<img src=\'' + data[0].cropped_image+'\' class="d-block w-1000" alt="...">';
                                    carousel += '<div class="carousel-caption d-none d-md-block">';
                                    carousel += '<p style="font-color: black;">' + data[0].value + '</p>';
                                    carousel += '</div></div>';
                                    for( i = 1; i < data.length; i++){
                                        carousel += '<div class="carousel-item" data-interval="false" >';
                                        carousel += '<img src=\'' + data[i].cropped_image+'\' class="d-block w-1000" alt="...">';
                                        carousel += '<div class="carousel-caption d-none d-md-block">';
                                        carousel += '<p style="font-color: black;">' + data[i].value + '</p>';
                                        carousel += '</div></div>';
                                    }
                                    carousel += '</div>';
                                    carousel += '<a class="carousel-control-prev" href="#carouselhr" role="button" data-slide="prev">';
                                    carousel += '<span class="carousel-control-prev-icon" aria-hidden="true"></span>';
                                    carousel += '<span class="sr-only">Previous</span></a>';
                                    carousel += '<a class="carousel-control-next" href="#carouselhr" role="button" data-slide="next">';
                                    carousel += '<span class="carousel-control-next-icon" aria-hidden="true"></span><span class="sr-only">Next</span></a>';
                                    carousel += '</div>';
                                    $('#forData').html(carousel);*/
                                    /*console.log('data l: ' + data.length);                                    
                                    let hand = '<ul class="list-unstyled">';
                                    for( i = 0; i < data.length; i++){
                                        console.log('path2: ' + decodeURI(data[i].cropped_image));
                                        hand += '<li style=" font-size: 2em"  id=\'' + i + '\' value=\'' + data[i].value + '\'>';
                                        hand += data[i].value + '<img src =\'' + data[i].cropped_image+'\' class="img-fluid" alt="Что-то не так с изображением">';
                                        hand += '</img>';
                                        hand += '<div><p>ТЕСТОВЫЙ ТЕКСТ</p></div></li>';
                                    }
                                    hand+= '</ul>';
                                    $('#forData').html(hand);*/

                                    let darktable = '<table id="verifyControlTable" class="table table-hover table-bordered">';
                                        darktable +='<tbody>';
                                    for( i = 0; i < data.length; i++){
                                        darktable += '<tr height="110"><th scrope="row">' + data[i].value + '</th>';
                                        darktable += '<td class="img" status="0" id=\'' + data[i].id + '\'><img src=\'' + data[i].cropped_image + '\' alt=\'Изображение со значением: '+ data[i].value +  '\' height="92" width="1071"/>';
                                        darktable += '</td>';
                                        darktable += '<td><ul class="list-unstyled">';
                                        darktable += '<li>' + data[i].task + '</li>'
                                        darktable += '<li>' + 'ФИО:' + '</li>'
                                        darktable += '<li>' + 'Никнейм:' + '</li>'
                                        darktable += '<li><a href="' + data[i].original_image +'">Ссылка на бланк</a></li>'
                                         '</ul></td>'
                                        darktable += '</tr>';

                                    }
                                    darktable += '</tbody></table>'
                                    darktable += '<div><button id="sendResult" type="submit"  class="btn btn-primary">Отправить</button></div>'
                                    
                                    $('#forData').html(darktable);
                                    $('#forData').bind('change', function(){
                                        alert('Что-то изменилось в твоей изни');
                                    })
                                    $('#verifyControlTable td.img').bind('click', function(){ 
                                        if($(this).attr("status") == 0){
                                            $(this).attr("status", 1);
                                            $(this).css('background-color', 'blue');
                                        }
                                        else if($(this).attr("status") == 1){
                                            $(this).attr("status", 2);
                                            $(this).css('background-color', 'red');
                                        }
                                        else if($(this).attr("status") == 2){
                                            $(this).attr("status", 0);
                                            $(this).css('background-color', 'inherit');
                                        }
                                    }); 
                                },
                                error: function(err){
                                    alert('something goes wrong ' + err);
                                }
                            })
                        } else {
                            alert('Не выбраны данные для проверки');
                        }
                   
                })
                $('#sbjlst').change(function(){
                    functions.getImages();
                }); 
                $("#prjlst").change(function(){ 
                    functions.getSubjects();
                    console.log('$("select#sbjlst").val():' + $("select#sbjlst").val());                    
               }); 
           })

           const functions = {
               getImages: function(){
                   $.ajax({
                        type: 'post',
                        url: '/admin/verifycontrol/getImages',
                        data: {
                            'subject_value' : $("select#sbjlst").val(),
                            'project_value' : $("select#prjlst").val()
                         },
                        success: function(data){
                            functions.setTable(data);
                        },
                        error: function(err){
                            console.log('Шо то не так( ' + err);
                        } 
                   });
               },

               getSubjects: function(){
                   $.ajax({ 
                        type: 'post',
                        url: '/admin/verifycontrol/getSubjects',
                        data: 'project_value=' + $("select#prjlst").val(),
                        success: function(data){
                            $("#sbjlst").remove();
                            let select = '<select id="sbjlst" class="form-control">'
                            $.each(data, function(key, value) { 
                                select += '<option value="' + JSON.stringify(value.subject_code) + '">' + JSON.stringify(value.subject_code) + '</option>';
                            })
                            select += '</select>';                           
                            $('#forSelectSbjs').html(select);
                            $('#sbjlst').bind('change',function(){ 
                                  functions.getImages();
                                }); 
                            //чтобы при смене предмета запускаласт функция с обновлением изображений
                            functions.getImages(); 
                        },
                        error: function(){
                            alert('Шо то не так( ' + document.getElementById('prjlst').value)
                        }
                    });
               },

               setTable: function(data){
                   console.log('data.length=' + data.length);
                            let numOfRows = 0, numOfCols = 10;
                            if(data.length%10 > 0){
                                numOfRows = Math.floor(data.length/10) + 1;
                                console.log('rows: ' + numOfRows);
                            } else {
                                console.log('acid rocket')
                                numOfRows = Math.floor(data.length/10);
                            }       
                            $('#tbody').remove();
                            let table_body = '<table id="preVerifyControlTable" class="table table-responsive table-bordered table-striped table-light"><tbody id="tbody">';
                            for(let i=0;i<numOfRows;i++){
                                table_body+='<tr>';
                                //console.log("rows it: " + i + 'and ' + Math.floor(data.length/10));
                                //проверка на не полные столбы
                                if(i == Math.floor(data.length/10)){ 
                                    numOfCols = data.length%10;
                                    //console.log("numcOfCols: " +numOfCols);
                                }
                                for(let j=0;j<numOfCols;j++){
                                    table_body +='<td value ="'+ data[+(''+i+j)].value +'" align="center">';
                                    //console.log(" numOfCols j: " + numOfCols + ' and ' + j);
                                    table_body +='<input id=\'' + (''+i+j) +'\'type=\'checkbox\' value=\''+ data[+(''+i+j)].value+ '\'>';
                                    table_body +=data[+(''+i+j)].value + '('+JSON.stringify(data[+(''+i+j)].count)+ ')';
                                    table_body +='</input>';
                                    table_body +='</td>';                                    
                                }
                                table_body+='</tr>';
                            }
                            table_body+='</tbody></table>'
                            $('#forTable').html(table_body);  
                            //Не работает тема, вернуться и доделать
                            $('#preVerifyControlTable input').bind('click',function(){ 
                                    $(this).css('background-color', 'green');
                            }); 
               }
           }
            
        